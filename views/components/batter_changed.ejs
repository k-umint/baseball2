<tr class="tablebody-tr">
    <td class="tabledata">
        <p>
            <button class="btn" type="button" data-toggle="collapse" data-target="#collapse<%- player.order %>"
                aria-expanded="false" aria-controls="collapse">
                <div id="name_<%- player.order %>" class="batter_name text-center">
                    <%- player.name %>
                </div>
            </button>
        </p>
        <div class="collapse" id="collapse<%- player.order %>">
            <div class="card card-body">
                <ul class="list-group">
                    <% let boxArr=[player.box_1,player.box_2,player.box_3,player.box_4,player.box_5,player.box_6]; %>
                        <% let boxArrMod=[] %>
                            <% boxArr.forEach((element,index)=> { %>
                                <% if(element){%>
                                    <% boxArrMod.push(element); %>
                                        <% } %>
                                            <% }); %>
                                                <% boxArrMod.push(""); %>
                                                    <% boxArrMod.forEach((element,index)=> { %>
                                                        <%- console.log("index : "+index) %> 
                                                        <li class=" list-group-item">
                                                            <p>第<%- index+1 %>打席</p>
                                                            <div id="box_<%- player.order %>-<%- index+1 %>"
                                                                class="hitting_result">
                                                                <a id="<%- player.order %>-<%- index+1 %>"
                                                                    class="text-center"
                                                                    href="/batterBox?boxId=<%- player.order %>-<%- index+1 %>"
                                                                    onclick="getBoxResult(event)">
                                                                    <%- element %>
                                                                </a>
                                                            </div>
                                                            </li>
                                                            <% }); %>
                </ul>
            </div>
        </div>
    </td>
</tr>